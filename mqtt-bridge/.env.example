# Google Cloud Configuration
# Required: GCP Project ID for accessing Secret Manager
GCP_PROJECT_ID=your-gcp-project-id

# For local development only (not needed in Cloud Run)
# Path to service account JSON file with Secret Manager access
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ==========================================
# All other configuration is stored in Google Secret Manager
# ==========================================
# The following secrets are automatically loaded at runtime:
#
# MQTT Configuration:
#   - mqtt-broker-url       (e.g., mqtts://your-broker.hivemq.cloud:8883)
#   - mqtt-username         (MQTT authentication username)
#   - mqtt-password         (MQTT authentication password)
#
# Pub/Sub Configuration:
#   - pubsub-topic                        (iot-sensor-readings)
#   - pubsub-topic-device-registration    (iot-device-registration)
#   - pubsub-subscription-commands        (device-commands-sub)
#
# No environment variables needed for these - they're fetched directly!

# Service Configuration
PORT=8080
NODE_ENV=production
LOG_LEVEL=info

# Buffer Configuration (optional)
# Time in milliseconds to buffer messages before batch publishing
BUFFER_INTERVAL_MS=60000
